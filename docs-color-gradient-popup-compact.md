# Color/Gradient Popup Compact Update

- Hid the Color tab from the tabbed fill popup while keeping the existing color logic available internally for solid color application.
- Made the popup open directly to the Gradient tab.
- Compacted the Gradient tab by reducing margins, spacing, preview height, stop control sizes, and hiding the preset folder list while keeping the quick preset strip.
- Updated disabled tab handling to use widget indexes so hiding the Color tab does not leave incorrect tab indexes.
