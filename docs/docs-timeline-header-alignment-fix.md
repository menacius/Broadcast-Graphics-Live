# Timeline Header Alignment Fix

- Reduced the timeline ruler/header height to match the LayerStack column header height.
- Removed the extra vertical spacer above the layer timeline rows.
- Kept the first timeline row aligned with the first layer-list row by sharing the same 44 px header offset and 28 px row height.
- Added a bottom divider to the compact timeline ruler so the header boundary remains clear after removing the excess space.
