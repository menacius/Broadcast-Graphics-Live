# Development Version 017 — Layer color visual consistency

- Group layers use a dedicated configurable timeline color.
- Canvas selection and hover bounding boxes use the corresponding layer type color.
- The active inline text editing bounding box uses the text layer color with a continuous solid outline.
- Multi-selection keeps per-layer colored outlines and uses the active layer color for the aggregate transform box.
